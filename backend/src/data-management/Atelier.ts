class Atelier {
    private id: number;
    private theme: Theme;
    private nbPlaces: number;

    constructor(id: number, theme: Theme, nbPlaces: number) {
        this.id = id;
        this.theme = theme;
        this.nbPlaces = nbPlaces;
    }
}